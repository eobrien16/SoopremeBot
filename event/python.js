exports.run = async (message) => {
    if (message.content.includes("python")) {
        await message.react("♿");
        await message.react("465268475510652938");
        await message.react("🇮");
        await message.react("🇸");
        await message.react("🇬");
        await message.react("🇦");
        return message.react("🇾");
        
    }
}